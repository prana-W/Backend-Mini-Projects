<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Generate Secret Message</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .header-section {
            text-align: center;
            margin-bottom: 30px;
        }

        .page-title {
            color: white;
            font-size: 2.5rem;
            margin-bottom: 10px;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        .page-subtitle {
            color: rgba(255, 255, 255, 0.9);
            font-size: 1.1rem;
            font-weight: 300;
        }

        form {
            background: white;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 500px;
            backdrop-filter: blur(10px);
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #333;
            font-weight: 600;
            font-size: 0.9rem;
            position: relative;
        }

        .required-field::after {
            content: "*";
            color: #e74c3c;
            margin-left: 4px;
        }

        input, textarea {
            width: 100%;
            padding: 12px 16px;
            margin-bottom: 20px;
            border: 2px solid #e1e5e9;
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background-color: #f8f9fa;
            font-family: inherit;
        }

        textarea {
            resize: vertical;
            min-height: 120px;
            line-height: 1.5;
        }

        input:focus, textarea:focus {
            outline: none;
            border-color: #667eea;
            background-color: white;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
            transform: translateY(-2px);
        }

        input:hover, textarea:hover {
            border-color: #667eea;
            background-color: white;
        }

        /* Special styling for number inputs */
        input[type="number"] {
            -moz-appearance: textfield;
        }

        input[type="number"]::-webkit-outer-spin-button,
        input[type="number"]::-webkit-inner-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }

        button {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-top: 10px;
        }

        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
        }

        button:active {
            transform: translateY(0);
        }

        /* Form sections */
        .form-section {
            margin-bottom: 25px;
            padding-bottom: 20px;
            border-bottom: 1px solid #e1e5e9;
        }

        .form-section:last-of-type {
            border-bottom: none;
            margin-bottom: 0;
        }

        .section-title {
            color: #667eea;
            font-size: 1rem;
            font-weight: 700;
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Security indicators */
        .security-note {
            background-color: #e8f5e8;
            border-left: 4px solid #4caf50;
            padding: 12px 16px;
            margin-bottom: 20px;
            font-size: 0.85rem;
            color: #2e7d32;
            border-radius: 4px;
        }

        .security-note::before {
            content: "🔒 ";
            margin-right: 5px;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            form {
                padding: 30px 20px;
                margin: 0 10px;
                max-width: none;
            }

            .page-title {
                font-size: 2rem;
            }

            .page-subtitle {
                font-size: 1rem;
            }
        }

        @media (max-width: 480px) {
            .page-title {
                font-size: 1.8rem;
            }

            textarea {
                min-height: 100px;
            }
        }

        /* Animation for form appearance */
        form {
            animation: slideUp 0.6s ease-out;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Input group styling for better organization */
        .input-group {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-bottom: 20px;
        }

        @media (max-width: 480px) {
            .input-group {
                grid-template-columns: 1fr;
                gap: 0;
            }
        }
    </style>
</head>
<body>

<div class="header-section">
    <h1 class="page-title">🔐 Secret Message</h1>
    <p class="page-subtitle">Create an encrypted message with custom expiration</p>
</div>

<form action="" method="post">
    <div class="form-section">
        <div class="section-title">Message Details</div>

        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="" placeholder="Give your secret message a title">

        <label for="message" class="required-field">Secret Message:</label>
        <textarea name="message" id="message" required placeholder="Type your secret message here... It will be encrypted and can only be viewed with the correct password."></textarea>
    </div>

    <div class="form-section">
        <div class="section-title">Security Settings</div>

        <div class="security-note">
            Your message will be encrypted using your password. Choose a strong password and share it securely.
        </div>

        <label for="password" class="required-field">Encryption Password:</label>
        <input type="password" id="password" name="password" required placeholder="Create a strong password to encrypt your message">
    </div>

    <div class="form-section">
        <div class="section-title">Access Controls</div>

        <div class="input-group">
            <div>
                <label for="maxAllowedViews">Maximum Views:</label>
                <input type="number" id="maxAllowedViews" name="maxAllowedViews" placeholder="e.g., 5" min="1">
            </div>
            <div>
                <label for="expiresIn">Expires In (Minutes):</label>
                <input type="number" id="expiresIn" name="expiresIn" placeholder="e.g., 60" min="1">
            </div>
        </div>
    </div>

    <button type="submit">🔐 Create Secret Message</button>
</form>

</body>
</html>